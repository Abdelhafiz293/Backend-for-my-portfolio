{
    "version": 2,
    "builds": [
        {
            "src": "index.js",
            "use": "@vercel/node"
        }
        {"src":"/(.*)", "dest": "/"}
    ],
    "routes": [
        { "src": "/(.*)", "dest": "/" }
    ]
}